# used for testing config-changed
version: '0.3'
monitors:
    local:
        processcount:
            all:
                max: 100
        misc:
            test-misc:
                name: Misc Monitor
                command: /usr/bin/foo
    remote:
        ssh:
            ssh:
                timeout: 10
